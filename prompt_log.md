## prompts

- create a design for a database of restaurants that tracks its id, name, public rating, private rating, etc. it must allow all 4 CRUD operations and have a feature where it displays the biggest gaps between public and private ratings
- code an app with python and SQLite following this design (paste modified result from ^ above)
- add a way to display the data in HTML
- add a way to update existing data without overriding it
- make an index.html file that automatically displays an up-to-date report without users needing to request that specifically
- make the following sanity checks
    - if a rating below 1 or above 5 is entered dont allow it
    - if a new restaurant is added with the exact same name as an existing one dont allow it just direct the user to the update option
- change the html to fit this colour palette (+ uploaded photo)